[pytest]
# Reine Unit-Tests der Parserfunktionen (Codereview Phase 23/Schritt 2,
# Befund B4) - ohne MuseScore, ohne Xvfb, ohne Container, in Millisekunden.
# Was der echte Prozess liefert, prueft weiterhin `GET /selftest` gegen das
# gebaute Image (Phase 21); die beiden ergaenzen sich, sie ersetzen sich nicht.
testpaths = tests
addopts = -q
