[pytest]
# Reine Unit-Tests der Parserfunktionen - ohne MuseScore, ohne Xvfb, ohne
# Container, in Millisekunden. Was der echte Prozess liefert, prueft
# weiterhin `GET /selftest` gegen das gebaute Image; die beiden ergaenzen
# sich, sie ersetzen sich nicht.
testpaths = tests
addopts = -q
