# Nur fuer die Unit-Tests des Sidecars. Das Laufzeit-Image installiert
# Flask ueber das Debian-Paket python3-flask (siehe Dockerfile) und
# braucht diese Datei nicht.
#
#   python -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/python -m pytest
flask>=3.1.3
pytest>=9.1.1
