<?xml version="1.0"?>
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
	<!-- Die Reihenfolge der Elemente ist durch die XSD fest vorgegeben und darf
	     nicht veraendert werden, sonst lehnt der App Store das Release ab. -->
	<id>scoreview</id>
	<name lang="de">ScoreView</name>
	<name lang="en">ScoreView</name>
	<summary lang="de">Notenblätter aus Files öffnen: einmal konvertiert, synchron abgespielt</summary>
	<summary lang="en">Open sheet music from Files: converted once, played back in sync</summary>
	<description lang="de"><![CDATA[
**Prototyp.** ScoreView zeigt MuseScore-Partituren (.mscz) direkt aus Files an
- als originalgetreue MuseScore-Notation (SVG) mit einem Wiedergabe-Cursor,
der synchron zur clientseitig synthetisierten MIDI-Wiedergabe mitläuft,
statt als starrer PDF-Export.

Eine hochgeladene .mscz-Datei wird über einen separaten MuseScore-Sidecar
einmalig zu Notenseiten (SVG), MIDI und Timing-Daten konvertiert und danach
gecacht: erneutes Öffnen derselben Datei rendert nicht neu, sondern bedient
sich aus dem Cache. Erst ein Re-Upload/eine Änderung der Datei stößt eine
erneute Konvertierung an. Voraussetzung ist ein erreichbarer
MuseScore-Sidecar (separat betrieben, nicht Teil dieses App-Pakets). Ein
SoundFont für die Wiedergabe muss nicht konfiguriert werden - die App liefert
das aus, das der Sidecar mitbringt; eine eigene Adresse lässt sich in den
Einstellungen hinterlegen.

Funktionsumfang: Taktnavigation, Loop, Zoom, Klick-auf-Note-Sprung, Mixer
(Lautstärke/Mute/Solo pro Stimme, Tempo), private Notizen an musikalischen
Ankern (Takt-Position, übersteht einen Re-Upload).
	]]></description>
	<description lang="en"><![CDATA[
**Prototype.** ScoreView displays MuseScore scores (.mscz) directly from
Files - as faithful MuseScore notation (SVG) with a playback cursor that
follows client-side synthesized MIDI playback in sync, instead of a static
PDF export.

An uploaded .mscz file is converted once, via a separate MuseScore sidecar,
to notation pages (SVG), MIDI and timing data, then cached: opening the
same file again does not re-render, it is served from the cache. Only a
re-upload/change of the file triggers a new conversion. Requires a
reachable MuseScore sidecar (operated separately, not part of this app
package). No SoundFont needs to be configured for playback - the app serves
the one the sidecar brings along; a custom address can be set in the
admin settings.

Feature set: measure navigation, loop, zoom, click-to-note jump, mixer
(per-voice volume/mute/solo, tempo), private annotations anchored to
musical position (survives a re-upload).
	]]></description>
	<version>0.0.24</version>
	<licence>agpl</licence>
	<author mail="hauffe.andreas@googlemail.com">Andreas Hauffe</author>
	<namespace>ScoreView</namespace>
	<category>multimedia</category>
	<bugs>https://github.com/AndiMb/scoreview/issues</bugs>
	<repository type="git">https://github.com/AndiMb/scoreview.git</repository>
	<dependencies>
		<php min-version="8.1" max-version="8.5"/>
		<database>sqlite</database>
		<database>mysql</database>
		<database>pgsql</database>
		<nextcloud min-version="31" max-version="35"/>
	</dependencies>
	<background-jobs>
		<!-- Periodisch und ohne Argument - anders als ConvertScoreJob, der
		     gezielt pro Datei ueber IJobList::add() eingereiht wird (siehe
		     AppInfo\Application). -->
		<job>OCA\ScoreView\BackgroundJob\CleanupOrphansJob</job>
	</background-jobs>
	<settings>
		<admin>OCA\ScoreView\Settings\AdminSettings</admin>
		<admin-section>OCA\ScoreView\Settings\Section</admin-section>
	</settings>
</info>
